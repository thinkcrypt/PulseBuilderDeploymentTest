import { fontOptions } from '@/components/library';

const data = [
	{
		name: 'primaryFont',
		label: 'Primary Font',
		type: 'select',
		options: fontOptions,
	},
	{
		name: 'secondaryFont',
		label: 'Secondary Font',
		type: 'select',
		options: fontOptions,
	},
];

export default data;
